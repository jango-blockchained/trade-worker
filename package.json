{
  "name": "trade-worker",
  "version": "1.0.0",
  "description": "Trade execution service for cryptocurrency exchanges",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init-db": "node scripts/init-db.js",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@binance/connector-typescript": "^0.3.16",
    "@cloudflare/puppeteer": "^1.1.0",
    "@hoox/shared": "workspace:*",
    "bybit-api": "^4.6.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260506.1",
    "bun-types": "^1.3.13",
    "wrangler": "^4.83.0"
  },
  "vitest": {
    "include": [
      "src/index.test.ts"
    ],
    "config": "./vitest.config.ts"
  }
}
